# Node 16 Result - Review IDs and Notification Batching

Status: complete in source; deployment pending the next controlled Core restart

## Delivered

- Every new group-profile owner notification includes `Review ID: GEX-<extraction_id>`.
- Price and candidate-task changes are classified as `immediate` and remain separate deliveries.
- Company-only and relationship-only changes are classified as `digest`.
- Pending digest items for the same owner are combined into one delivery per worker cycle.
- Each underlying notification remains an independent durable database row and is marked sent/retry together with its batch outcome.
- Existing owner review commands continue to use the numeric extraction ID shown in the notification.

## Verification

- Notification batching verification passed: 3 queued rows produced 2 deliveries, with 2 digest rows combined and 1 price row immediate.
- All 3 durable rows were marked sent after mocked successful delivery.
- Group business extraction, owner review commands, group profile foundation and member personal-memory regressions passed.
- Python compilation passed.
- All verification used temporary databases and mocked WeChat delivery.

## Formal Database Snapshot

- `group_message_extractions`: 4
- `group_profile_notifications`: 2
- `group_extraction_reviews`: 0
- `outbox`: 140
- `raw_web_evidence`: 16
- `lead_pre_pool`: 14
- `lead_add_queue`: 13

No formal database writes or real WeChat actions were performed by Node 16.

## Next

- Add an explicit digest time window and maximum digest size if owner notification volume grows.
- Deploy Node 16 together with the allowlist formal-reply fix during the next administrator-controlled 8766 restart.
