# Node 18 Result - Safe Historical Group Backfill

Status: complete in source and dry-run; formal backfill not applied

## Mainline Position

- Upstream: existing inbound group messages for groups that already have authoritative `group_profiles`.
- Processing: existing `extract_group_business_elements()` and `extract_group_message()` paths only.
- Output: existing `group_message_extractions`, `group_profile_events`, memberships and threads.
- Downstream: existing owner review workflow; historical runs suppress notifications.
- Core boundary: no customer memory, authorization, outbox, action request or WeChat write.

## Delivered

- Explicit `--confirm-backfill GROUP_HISTORY_BACKFILL` gate is required with `--apply`.
- Optional chatroom and before/after ISO time filters bound each batch.
- Stable batch ID is derived from the selected message IDs.
- Every run reports protected-table counts before and after.
- Apply aborts and rolls back if any protected-table invariant changes.
- Historical extraction continues to suppress owner notifications.
- Existing unique message extraction key makes repeated apply idempotent.

## Verification

- Live database dry-run selected 85 historical messages: 34 relevant and 51 noise.
- Live dry-run made zero writes and queued zero notifications.
- Temporary SQLite backup dry-run made zero writes.
- Temporary apply wrote exactly 85 extraction rows.
- Repeated temporary apply selected 0 rows and made no changes.
- Contacts, customer profiles, customer authorizations, outbox and action requests remained unchanged.
- Existing group extraction, review command, profile foundation and member personal-memory regressions passed.
- No real WeChat action occurred.

## Formal Database Decision

No formal historical backfill was applied. A separate operator decision is required before using `--apply` on the authoritative database.

## Next

Deepen group business profiles using reviewed/effective extraction values while preserving original evidence and group isolation.
