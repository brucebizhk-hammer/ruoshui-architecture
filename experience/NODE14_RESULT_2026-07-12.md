# Node 14 Result - Group Extraction Review Workflow

Status: complete

## Delivered

- Added pending, confirmed, corrected and rejected review states for group-message extractions.
- Added append-only `group_extraction_reviews` audit records.
- Original extracted elements remain immutable; corrected effective elements are stored separately.
- Rejected records retain evidence but expose no effective business elements.
- Added `GET /groups/extractions` for review queues.
- Added dry-run-first `POST /groups/extractions/review` requiring `confirm=true` to apply.
- Group profile snapshots now show both original and effective reviewed values.

## Verification

- Default review call produced a preview without a database write.
- Confirmed correction changed the effective price from 1280 to 1290 while preserving 1280 as original evidence.
- Review created a separate audit event.
- Group extraction, profile foundation and member personal-memory tests passed.
- No WeChat message or group action occurred during review testing.

## Next

Add owner-friendly review commands and notification batching so corrections can be made from a concise private workflow.
