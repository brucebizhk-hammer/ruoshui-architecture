# Node 11 Result - Group Customer Profile Foundation

Status: complete

## Delivered

- Added independent `group_profiles` with G0-G4 level, purpose, business line, project, lifecycle and structured profile data.
- Added `group_memberships` linking each group member wxid to one personal contact while retaining group-specific role and identity confidence.
- Added `group_policies` separating collection, mention reply, proactive messaging, allowed sources/actions and blocked topics.
- Added append-only `group_profile_events` for profile, member and policy changes.
- Added Core APIs:
  - `GET /groups/profile`
  - `POST /groups/profile/upsert`
  - `POST /groups/members/upsert`
  - `POST /groups/policy/upsert`
- All write APIs default to dry-run and require `confirm=true`.

## Formal Migration

- Created minimal profiles for the three currently approved groups.
- Two named business groups are G2; the group awaiting a confirmed name is G1.
- Collection is enabled, replies require an explicit mention, proactive messaging is disabled.
- Reply sources are limited to the current group profile and public web.
- Local customer database, cross-group memory, private chats, contract commitments and payment commitments are blocked.

## Verification

- `tools/verify_group_profile_foundation.py`: passed.
- `tools/verify_group_member_personal_memory.py`: passed.
- Core and middleware restarted through the elevated recovery task and returned healthy.
- No WeChat message or group action was performed by migration or verification.

## Next

Build the group-message extraction worker that turns business-relevant messages into evidence-backed group profile updates while filtering noise.
